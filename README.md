## Run locally (Windows PowerShell)

1. Open PowerShell in this folder (path). 
2. Start PHP's built-in server:

```powershell
php -S localhost:8000
```

3. Open your browser and go to: `http://localhost:8000/index.php`

